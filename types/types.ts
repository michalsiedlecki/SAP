export type PageMeta = {
  name: string;
  url: string;
  topCategory: string;
  sideCategory: string;
};

export type E2ESolution = {
  icon: string;
  title: string;
  description: string;
  url: string;
};